get:
  operationId: VitrineController_getEventosExternos
  parameters: []
  responses:
    "200":
      description: "Lista de eventos externos"
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../../components/schemas.yaml#/Evento"
  security:
    - bearer: []
  summary: "Retorna os eventos externos"
  tags:
    - Vitrine
