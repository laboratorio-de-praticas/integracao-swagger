get:
  summary: Lista os representantes disponíveis na vitrine
  tags:
    - Vitrine
  responses:
    "200":
      description: Lista de representantes
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../../components/schemas.yaml#/Representante"